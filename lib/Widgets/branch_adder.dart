import 'package:flutter/material.dart';

class BranchAdder extends StatelessWidget {
  const BranchAdder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
